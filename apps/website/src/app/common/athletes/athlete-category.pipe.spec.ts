import { AthleteCategoryPipe } from './athlete-category.pipe';

describe('AthleteCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new AthleteCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
